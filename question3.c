#include<stdio.h>
//write a pogram to calculate area of circle (side is given)


int main()
{
    float r;
  // r is radius of circle
  printf("enter radius :  ");
  scanf("%f",&r);
  printf("area of circle is %f",3.14*r*r);

  return 0;
} 
